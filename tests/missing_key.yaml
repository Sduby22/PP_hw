# working dir of scripts and main interpreter.
pwd: .

# runtime config
config:
  # wait forever instead of wait a fixed time.
  wait-forever: true

# scripts dirs
scripts: 
  dirs: 
    - ./scripts/
